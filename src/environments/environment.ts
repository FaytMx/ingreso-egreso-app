// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyBtv9b7eiQPKJ_b55_PEEb6LH5oFbTIm1I',
    authDomain: 'ingreso-egreso-app-b894c.firebaseapp.com',
    databaseURL: 'https://ingreso-egreso-app-b894c.firebaseio.com',
    projectId: 'ingreso-egreso-app-b894c',
    storageBucket: 'ingreso-egreso-app-b894c.appspot.com',
    messagingSenderId: '687908315474',
    appId: '1:687908315474:web:659df559744a1018'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
